---
const navLinks = [
  { label: 'Inicio',         href: '/' },
  { label: 'Quiénes Somos',  href: '/quienes-somos' },
  { label: 'Servicios',      href: '/servicios' },
  { label: 'Blog',           href: '/blog' },
  { label: 'Preguntas',      href: '/preguntas-frecuentes' },
]

const currentPath = Astro.url.pathname
---

<header class="bg-white border-b border-neutral-200 shadow-sm sticky top-0 z-50">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-24">

      <!-- Logo -->
      <a href="/" class="flex items-center">
        <img
          src="/logoGrupoSinergia.svg"
          alt="Grupo Sinergia Consultores"
          style="height: 92px; width: auto;"
        />
      </a>

      <!-- Desktop nav -->
      <nav class="hidden md:flex items-center gap-1">
        {navLinks.map(link => (
          <a
            href={link.href}
            class:list={[
              'px-4 py-2 rounded text-sm font-medium transition-colors',
              currentPath === link.href
                ? 'text-brand-800 bg-brand-50'
                : 'text-neutral-500 hover:text-brand-800 hover:bg-brand-50'
            ]}
          >
            {link.label}
          </a>
        ))}

        <a
          href="/contacto"
          class="ml-4 px-5 py-2 rounded bg-brand-800 text-white text-sm font-semibold hover:bg-brand-700 transition-colors"
        >
          Contáctenos
        </a>
      </nav>

      <!-- Mobile menu button -->
      <button
        id="menu-btn"
        class="md:hidden text-neutral-500 hover:text-brand-800 p-2"
        aria-label="Abrir menú"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
            d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>

    <!-- Mobile nav -->
    <nav id="mobile-menu" class="hidden md:hidden pb-4 border-t border-neutral-100 mt-1 pt-3">
      {navLinks.map(link => (
        <a
          href={link.href}
          class:list={[
            'block px-3 py-2 rounded text-sm font-medium transition-colors mb-1',
            currentPath === link.href
              ? 'text-brand-800 bg-brand-50'
              : 'text-neutral-500 hover:text-brand-800 hover:bg-brand-50'
          ]}
        >
          {link.label}
        </a>
      ))}
      <a
        href="/contacto"
        class="block mt-2 px-3 py-2 rounded bg-brand-800 text-white text-sm font-semibold text-center hover:bg-brand-700 transition-colors"
      >
        Contáctenos
      </a>
    </nav>
  </div>
</header>

<script>
  const btn = document.getElementById('menu-btn')
  const menu = document.getElementById('mobile-menu')
  btn?.addEventListener('click', () => menu?.classList.toggle('hidden'))
</script>
